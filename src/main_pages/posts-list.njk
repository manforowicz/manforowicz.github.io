---
title: Marcin's Website
nav_bar_name: Posts
---

<main>
    <h1>Posts</h1>
    <ol>
        {% for post in collections.posts | reverse %}
            <li>
                <a href={{ post.url }}>{{ post.data.title }}
                    <div><i>{{ post.date | postDate }}</i></div>
                </a>
            </li>
        {% endfor %}
    </ol>
</main>